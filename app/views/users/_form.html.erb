

<%= form_for user do |u| %>
	<%= u.label :name %>
	<%= u.text_field :name %>

	<%= u.label :password %>
	<%= u.text_field :password %>

	<%= u.label :password_confirmation %>
	<%= u.text_field :password_confirmation %>

	<%= u.submit %>
<% end %>
