<% if logged_in? %>

  <h1>Hello!, <%= current_user.name %></h1>
  
  <%= form_tag logout_path do %>
    <%= submit_tag 'Logout' %>
  <% end %>
  
<% else %>

  <%= link_to 'Signup', signup_path %>
  
<% end %>