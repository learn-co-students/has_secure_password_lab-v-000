<h1>Login</h1>
<h4>or <%= link_to "Signup", signup_url %></h4>

<%= form_for(@user, :url => '/login') do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit "Login" %>
<% end %>
