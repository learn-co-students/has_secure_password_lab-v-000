<%= form_for :user, controller: 'sessions', action: 'new' do |f| %>
	Username: <%= f.text_field :name %><br><br>
	Password: <%= f.password_field :password %><br>

	<%= f.submit "Login" %>
	Need an account?
	<%= link_to(controller: 'users', action: 'new') {'sign up'} %>
	<% end %>